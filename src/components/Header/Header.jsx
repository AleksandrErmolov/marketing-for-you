import s from "./Header.module.css"

function Header (props) {
    return (
        <header className={s.header}>
<p>dsfsdfs</p>
            
            
        </header>
    );
}

export default Header;